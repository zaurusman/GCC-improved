int c = 1 b;
bool bool1 = false;
bool bool2 = true;
bool boolGadol;
while (c < 10){
    c = c + 1;
    int a = 100;
    while (a > c){
        boolGadol = a / c * c == a;
        int i = 1;
        while (i < c) {
            if (false or (true and not (bool1 or bool2 and not boolGadol))){
                a = a - 1;
            } else {
                print("Somethings");
            }
            i = i + 1;
        }
        bool1 = not bool2;
        bool2 = bool1 and not boolGadol;
        if (bool1){
            a = a / 2 + 1;
        } else if (not bool2) {
            printi(c / a * a + c + 3 * 2);
        }
        printi(a * a / c / c + c / a * 2 / 3 + 4 * c / (a + 5));
    }
}